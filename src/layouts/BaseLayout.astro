---
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import { withBase } from '../lib/url';
import '../styles/global.css';

interface Props {
  title: string;
  description?: string;
  active: 'home' | 'games' | 'reviews' | 'wall' | 'players' | 'wins' | 'rules';
}

const { title, description = 'BaileyCon — Third Annual, a private board-game convention at Knight Watch Games, San Antonio TX.', active } = Astro.props;
---

<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>{title}</title>
  <meta name="description" content={description} />
  <link rel="icon" href={withBase('/favicon.svg')} type="image/svg+xml" />

  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link
    href="https://fonts.googleapis.com/css2?family=Bangers&family=Playfair+Display:ital,wght@0,700;0,900;1,700;1,900&family=Space+Grotesk:wght@400;500;700&family=Oswald:wght@700&display=swap"
    rel="stylesheet"
  />
</head>
<body>
  <Header active={active} />
  <main>
    <slot />
  </main>
  <Footer />
</body>
</html>
